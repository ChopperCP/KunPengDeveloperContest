<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.huawei.dao.UserMapper">
    <!--增加一个Book-->
    <insert id="addUser" parameterType="User">
        insert into kunpeng.user(wechat_id, privilege, user_QR_str, phone_number, real_name)
        values (#{wechat_id}, #{privilege}, #{user_QR_str}, #{phone_number}, #{real_name})
    </insert>

    <update id="updateUser" parameterType="User">
        update kunpeng.user
        set wechat_id=#{wechat_id},
            privilege=#{privilege},
            user_QR_str=#{user_QR_str},
            phone_number=#{phone_number},
            real_name=#{real_name}
        where wechat_id = #{wechat_id}
    </update>
    <select id="queryUserBywechatid" resultType="User">
        select *
        from kunpeng.user
        where wechat_id = #{wechat_id}
    </select>
    <select id="queryalluser" resultType="User">
        select *
        from user
    </select>
    <select id="queryuserByname" resultType="User">
        select *
        from user
        where real_name = #{real_name}
    </select>

</mapper>